To run this application use these commands from the project root folder:

Pre-requisites: Java 21 installed and maven installed.

1. Build
mvn clean compile

2. Run
java -cp target/classes com.example.Main
